[pytest]
testpaths = src/testsuites
pythonpath = src
addopts = -q --strict-markers

markers =
    smoke: quick sanity tests
    regression: full validation tests
    integration: interface and communication tests
    e2e: end-to-end workflow tests